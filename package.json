{
  "name": "stah-scrape-ci",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "devDependencies": {
    "@types/node": "^22.15.30",
    "wrangler": "^4.4.0"
  }
}