{
  "name": "stah-scrape-ci",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "devDependencies": {
    "@types/node": "^24.3.1",
    "wrangler": "^4.34.0"
  }
}